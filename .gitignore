#Add _bmad to the .gitignore file to ignore the bmad directory and its contents.
**/_bmad
#Add _bmad-output to the .gitignore file to ignore the bmad-output directory and its contents.
**/_bmad-output
#Add .vs to the .gitignore file to ignore the vs directory and its contents.
**/.vs
#Add .vscode to the .gitignore file to ignore the vscode directory and its contents.
**/.vscode
#Add packages to the .gitignore file to ignore the packages directory and its contents.
**/packages
#Add .github to the .gitignore file to ignore the github directory and its contents.
**/.github
#Add Visual Studio & C# ASP.NET Core to the .gitignore file to ignore files generated by Visual Studio and ASP.NET Core.
# Visual Studio
.vs/
# ASP.NET Core
bin/
obj/
